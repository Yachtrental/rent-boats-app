name: ClickAndBoat Palma Audit

on:
  pull_request:
    paths:
      - 'tools/clickandboat-audit/**'
      - '.github/workflows/clickandboat-palma-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  facet-builder:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r tools/clickandboat-audit/requirements.txt

      - name: Validate facet builder
        run: python tools/clickandboat-audit/test_facet_builder.py

      - name: Upload validation
        uses: actions/upload-artifact@v4
        with:
          name: clickandboat-facet-tests
          path: tools/clickandboat-audit/facet-test-output
          if-no-files-found: error
          retention-days: 7
